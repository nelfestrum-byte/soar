# Content-venv dependency contract (soar/runtime_contract.py::CONTRACT is
# the source of truth for what each entry means — protocol vs vendor).
# Unchanged by Phase 3 of the entity-model plan: the 24 built-in connectors
# that used to live in soar/connectors/ moved to a separate content-pack
# repo (docs/compose/specs/2026-07-30-content-as-contentpack-design.md),
# but the packages they import are still part of the platform's dependency
# contract — the contract describes what content-venv guarantees, not
# where the code that uses it happens to live on disk (see [S9] in that
# spec). Extending this list is a platform release: add here AND to
# runtime_contract.py::CONTRACT in the same commit, with tests
# (tests/soar/test_runtime_contract.py).
loguru>=0.7.0
pyyaml>=6.0
elasticsearch>=8.0.0
vt-py>=0.17.0
paramiko>=3.4.0
ldap3>=2.9.1
requests>=2.31.0
httpx>=0.27.0
psycopg2-binary>=2.9.9
pymysql>=1.1.0
pymssql>=2.2.11
aiogram>=3.7.0
aiosmtplib>=3.0.1
pywinrm>=0.5.0
smbprotocol>=1.13.0
shodan>=1.31.0
pymisp>=2.4.180
