# Plan: Bug Fixes v0.4–v0.5

> Spec: `docs/compose/specs/2026-07-03-bugfixes-design.md`

## Порядок: test-first, один баг — один коммит

---

- [x] Write plan file

## B1: Cancel race: CANCELLED → FAILED

**Файл:** `orchestrator/core/worker.py`

- [ ] Тест: `test_execute_cancel_not_overwritten_by_failed` в `tests/orchestrator/test_worker_execute.py`
  - Мок-процесс возвращает returncode=1
  - Перед возвратом `communicate()` — устанавливаем статус CANCELLED в store
  - После `_execute` — статус должен оставаться CANCELLED
- [ ] Фикс: после `communicate()`, до `if proc.returncode`, перечитать store; если CANCELLED — не менять статус

## B2: MySQL SQL concat

**Файл:** `soar/connectors/mysql/mysql.py:77`

- [ ] Тест: `tests/soar/test_mysql_connector.py` — проверить, что `tables()` строит запрос с бэктиками
- [ ] Фикс: `rows = self.execute(f"SHOW TABLES FROM \`{db}\`")`

## B4: result_data всегда null

**Файлы:** `orchestrator/core/worker.py`, `orchestrator/core/subprocess_runner.py`

- [ ] Тест: `test_execute_result_data_parsed` — мок-процесс, log_path указан, лог-файл содержит JSON с `{"success": true, "data": {"foo": 1}}` → `job.result_data == {"foo": 1}`
- [ ] Тест: `test_execute_result_data_non_json_ignored` — лог содержит не-JSON последнюю строку → `result_data` is None
- [ ] Фикс: после `communicate()`, читать `job.log_path`, взять последнюю непустую строку, попробовать `json.loads()`, если успешно — записать в `result_data`/`result_error`

## B3: RedisQueue concurrency serialization

**Файл:** `orchestrator/core/queue/redis_queue.py`

- [ ] Тест: `test_redis_queue_concurrency_preserved` — push job с `concurrency=QUEUE`, pop → `concurrency == ConcurrencyPolicy.QUEUE`
- [ ] Фикс: добавить `"concurrency": job.concurrency.value` в `push()`; восстанавливать через `ConcurrencyPolicy(item["concurrency"])` в `pop()`
- [ ] Fallback: если `"concurrency"` не в item — `ConcurrencyPolicy.FORBID`

## B7: Приватные поля из роутов

**Файлы:** `orchestrator/core/job_manager.py`, `orchestrator/core/queue/base.py`, `orchestrator/core/queue/memory.py`, `orchestrator/core/queue/redis_queue.py`, `orchestrator/api/workflows.py`, `orchestrator/api/webhooks.py`, `orchestrator/api/status.py`

- [ ] Тест: `test_job_manager_list_metas`, `test_job_manager_get_meta`
- [ ] Фикс `JobManager`: добавить `get_meta(name) -> WorkflowMeta | None`, `list_metas() -> list[WorkflowMeta]`
- [ ] Фикс `AbstractJobQueue`: добавить абстрактный `health() -> dict`
- [ ] Фикс `InMemoryQueue`: реализовать `health()` → `{"connected": True, "size": int}`
- [ ] Фикс `RedisQueue`: реализовать `health()` → `{"connected": bool, "size": int}` (ping)
- [ ] Фикс `workflows.py:64`: `job_manager._metas.values()` → `job_manager.list_metas()`
- [ ] Фикс `webhooks.py:11`: `job_manager._metas.get()` → `job_manager.get_meta()`
- [ ] Фикс `status.py:24-25`: `queue._ensure_connected()` + `queue._redis.ping()` → `queue.health()`

## B5: Rate limiter за nginx (trusted_proxies)

**Файлы:** `orchestrator/config.py`, `orchestrator/main.py`, `orchestrator/config.yaml`

- [ ] Тест: middleware получает X-Real-IP, trusted_proxies=["127.0.0.1"] → rate limit по X-Real-IP; без trusted_proxies — X-Real-IP игнорируется
- [ ] Фикс `config.py`: добавить `ServerConfig` с `trusted_proxies: list[str] = []`; добавить в `OrchestratorConfig`
- [ ] Фикс `main.py`: в `rate_limit_middleware` — если `client_ip in trusted_proxies`, брать IP из `X-Real-IP` / `X-Forwarded-For`
- [ ] Фикс `config.yaml`: добавить `server:\n  trusted_proxies: []`

## B6: SSRF DNS resolve

**Файл:** `orchestrator/api/connectors.py`

- [ ] Тест: `test_ssrf_domain_resolves_to_private` — домен, резолвящийся в private IP, должен бросать 400
- [ ] Фикс: в `_validate_external_url()` добавить `socket.getaddrinfo()`, проверять каждый A-адрес через `ipaddress`
- [ ] Фикс: httpx-клиент `follow_redirects=False`
