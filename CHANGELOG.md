# Changelog

История версий SOAR. Текущее состояние проекта — в [AGENTS.md](AGENTS.md).

- **v0.1** (2026-06-30) — Minimal SOAR: connectors, actions, workflows, orchestrator, UI, Docker deploy
- **v0.2** (2026-07-01) — Enterprise SOAR connectors: SSH, AD, FreeIPA, Elastic, SecurityOnion, Wazuh, PostgreSQL/MySQL/MSSQL, Telegram, SMTP, VirusTotal, Abuse.ch, File
- **v0.3** (2026-07-02) — Security hardening: 11 critical + 12 important fixes. SSRF protection, Zip Slip prevention, SQL/LDAP injection fixes, path traversal guards, rate limiting, subprocess lifecycle management
- **v0.4** (2026-07-02) — BIG FIX: DELETE workflow state cleanup, webhook token in API responses, additional connector hardening. Rate limiting (120 req/60s), SSRF protection for OpenAPI preview, SSH WarningPolicy, WinRM SSL verification, Wazuh secure defaults, MySQL identifier validation, SMTP attachment existence check, subprocess config path resolution, workflow registry filename-based keys, Redis queue triggered_at serialization, worker cancel skip + finally cleanup, CORSMiddleware credentials=False
- **v0.5** (2026-07-03) — Reliability + Bug fixes. ConcurrencyPolicy.QUEUE в Worker (busy-wait), `concurrency` в WorkflowJob и enqueue, `JobStore.recover_on_startup()`. Bug fixes (7): B1 cancel race, B2 MySQL backtick, B3 RedisQueue concurrency serialization, B4 result_data from log, B5 trusted_proxies rate limiter, B6 SSRF DNS resolve, B7 public API for private fields
- **v0.5.1** (2026-07-06, feature/auth) — Authentication: JWT (HS256, access 30min + refresh 7d, rotation), API keys (M2M, `soar_<hex>`), RBAC (admin/analyst/viewer/service), bcrypt пароли, SQLAlchemy 2.0 async DB layer, CORS с credentials, login rate limiter (5/60s), backward-compat auth-disabled mode. 21 тест, 341/342 passed.
- **v0.5.2** (2026-07-09) — IRP-интеграция (SOC Core Control): IRPConnector (`soar/connectors/irp/`) + четыре workflow поверх контракта `docs/integration/soc-core-integration-contract.md` — `alert_triage` (pull ES → триаж → ingest, чанк-реплей догона), `irp-events` (webhook-приёмник событий IRP), `irp_reconcile` (поллер-страховка), `respond_basic` (первый response-плейбук, без деструктива). Durable watermark/дедуп (`soar/tools/watermark.py`), кэш политик триажа из SOC Core settings API (`soar/tools/triage_policy.py`). Конфиг — секция `irp:` в `config.yaml`, `enabled: false` по умолчанию (shadow-режим обязателен до cutover)
- **v0.5.3** (2026-07-10) — Откат IRP-интеграции (v0.5.2): удалён `IRPConnector`, четыре workflow (`alert_triage`, `irp-events`, `irp_reconcile`, `respond_basic`), `soar/tools/{irp_settings,irp_dispatch,triage_policy}.py`, секция `irp:` в обоих `config.yaml`. Причина: бизнес-логика и настройки интеграции обходили API-редактируемые поверхности (см. "Архитектурный принцип: движок vs поведение" в AGENTS.md); решено не чинить точечно, а строить следующую интеграцию через API (см. v0.6). Контракт (`docs/integration/soc-core-integration-contract.md`) сохранён как референс. Добавлен read-only `GET /tools`/`GET /tools/{name}` (AST-интроспекция `soar/tools/`, без импорта модулей) — обнаружимость оставшихся generic-примитивов (`OpenAPIGenerator`, `WatermarkStore`/`SeenStore`) для того, кто будет строить интеграции дальше
- **v0.6** (planned) — Tooling: `CachedHttpClient` в `soar/tools/` (InMemory/Redis, TTL per-domain, request logging) для threat-intel actions; расширение `/status` in-memory метриками per-workflow (FP-rate, MTTR)
- **v0.7** (2026-07-17) — PostgreSQL migration с обратной совместимостью SQLite. Общий `database:` конфиг (`url`, `table_prefix`) теперь используется и auth-таблицами, и (опционально) job-историей — единая точка переключения бэкенда, без дублирования настроек. `table_prefix` — префикс имён таблиц для конфликтов при совместном использовании БД (`configure_table_prefix()`/`prefixed()`/`fk()` в `orchestrator/db/base.py`, применяется до импорта ORM-моделей). `AbstractJobStore` (`orchestrator/store/base.py`) — интерфейс, `InMemoryJobStore` (бывший `JobStore`, дефолт) и новый `SQLJobStore` (persistent, `jobs.persistence: sql`) — закрывает Known Limitations #3/#4 (crash recovery, история jobs переживает рестарт) при включении `sql`. Alembic-миграции (`alembic/`) — production-путь для будущих schema-изменений (`create_all()` остаётся для dev/test, только создаёт отсутствующие таблицы). `deploy/stage` — сервис `postgres`, пример конфига с Postgres + `table_prefix` + `jobs.persistence: sql`. Проверено end-to-end на реальном Docker Compose стенде: job сохраняется в Postgres с префиксом, переживает `docker compose restart` оркестратора.
- **v0.8** (2026-07-18) — API request logging + audit trail. `access_log_middleware` (`orchestrator/main.py`, зарегистрирован последним → самый внешний слой) — `request_id`/`X-Request-ID` на каждый запрос, структурированная строка лога (method/path/status/duration_ms/client_ip/user_id) через `logger.contextualize`. Ранее молчаливые отказы (401/403 в auth, 429 rate-limit, невалидный webhook-токен) теперь пишут `logger.warning`. Новая таблица `audit_log` (`orchestrator/audit/`, та же БД что auth/job-история, миграция `alembic/versions/3067dea7c75b_add_audit_log_table.py`) — 14 мутирующих роутов (workflows/actions/connectors CRUD, api-key create/delete, job cancel) пишут туда явно через `audit.service.record()`, читается через `GET /audit-log` (admin-only, фильтры + пагинация). `GitManager.commit()` принимает `author_name`/`author_email` override — git-коммиты через API теперь атрибутируются реальному пользователю, а не фиксированному `config.git.author_name`. UI (`ui/src/views/AuditLog.vue`) — раздел **Audit Log** в навигации (admin-only) с фильтрами, плюс кнопка **Audit** на строках Workflows/Actions/Connectors/Jobs/ApiKeys, ведущая на предзаполненный фильтр по конкретному ресурсу (без фильтра `GET /audit-log` отдаёт все типы ресурсов вперемешку — это осознанный дизайн, не баг). Стенд `deploy/stage` пересобран и провалидирован end-to-end: `alembic_version` на `3067dea7c75b (head)`, `GET /status`/`GET /audit-log` живые (см. Known Limitations #6/#7 для двух ограничений, найденных при проверке — JWT-акторы без username в audit-записях, и edge-case в `GitManager` с untracked-файлами, из-за которого audit-запись иногда пропускается).
- **v0.9** (2026-07-21/22) — Auth CLI fix + полный user lifecycle через API/UI; auth реально включена на стенде. `orchestrator/auth/cli.py` больше не читает отдельную `SOAR_DB_URL` — резолвит `database.url`/`table_prefix` из `SOAR_CONFIG` (как `main.py`), вызывает `configure_table_prefix()` до импорта моделей; баг чинился потому, что на стенде (`table_prefix: "stage_"`) `create-user` тихо писал в непрефиксованную `users`, которую сервис не читает. Добавлены CLI `deactivate-user`/`activate-user` (soft-delete через `User.is_active`). Новый публичный `GET /health` (без auth, liveness-проба для Docker healthcheck — `GET /status` теперь требует роль, healthcheck на `/status` ронял контейнер в unhealthy сразу после включения auth). `deploy/stage/config.yaml` получил `auth.secret_key` — auth на стенде больше не выключена (был анонимный admin). Новое: `/auth/users` (`POST`/`GET`/`PATCH {id}`, admin-only, зеркалит `/auth/keys`) — создание, список, смена role/is_active/password одним `PATCH`, self-lockout guard (409 на попытку деактивировать себя), audit-записи `user.create`/`user.update` (пароль никогда не в `detail`). UI: `ui/src/views/Users.vue`, пункт «Users» в навбаре (admin-only). CLI остаётся для bootstrap первого admin'а. Известное ограничение — Known Limitations #8 (нет защиты от деактивации/разжалования последнего admin'а). Проверено end-to-end на пересобранном стенде: полный цикл create/list/role-change/deactivate/reactivate/password-reset через `curl`, self-lockout, `GET /audit-log`.
- **v0.10** (2026-07-22) — Deploy CLI (`soarctl`) для распространения/установки/управления инстансом под air-gap. Два слоя: host-layer `deploy/soarctl` (тонкий argparse-shim, stdlib-only) + `deploy/soarctl_lib/` (package/install/init/up/down/restart/status/logs/migrate/users/backup/doctor — каждый модуль строит argv и вызывает единственную точку `runner.run()`). Дистрибуция без реестра образов: `soarctl package` собирает `soar-orchestrator`/`soar-ui` + пуллит `redis:7-alpine`/`postgres:16-alpine`, `docker save` всех четырёх в один tar вместе с compose-файлом/config-темплейтом/самим `soarctl` — `soarctl install` только `docker load` + распаковка, без единого сетевого вызова. Новый деплой-профиль `deploy/prod/` (параллельно `deploy/stage/`, которая не тронута) — `image:` вместо `build:`, `config.yaml` bind-mount (не запекается в образ, в отличие от stage), секреты (`AUTH_SECRET_KEY`/`POSTGRES_PASSWORD`/`SOAR_WEBHOOK_TOKEN`) генерируются `soarctl init` (`secrets.token_hex`) в `.env`/`config.yaml`, ни один не коммитится. `soarctl migrate --fresh|--upgrade` — два явных алиаса на `alembic stamp|upgrade head`, без авто-детекта (см. Known Limitations, дуальность `create_all()`/Alembic остаётся нерешённой). `soarctl backup create/restore` — БД (`pg_dump`/`psql` через `compose exec`) + `soar-data` volume (через alpine-контейнер, tar по stdin/stdout, без bind-mount временной директории — ломается на Windows из-за конфликта буквы диска с `host:container` `-v`-синтаксисом) одним архивом; `restore` требует явного `confirm=True`. `soarctl install` на уже `init`-нутом инстансе только обновляет `SOAR_VERSION` в `.env`, не трогая секреты (иначе апгрейд ломает доступ к своей же Postgres). Мультиинстансность явно вне scope — Known Limitations #9. 70 новых тестов (`tests/deploy/`, все на моках `subprocess`), полный набор + `ruff check` без регрессий. Проверено end-to-end на реальном Docker дважды: (1) `package` (реальный `docker build`/`pull`/`save`) → `install` (реальный `docker load`) → `init` → `doctor` → `up` (все 4 контейнера healthy) → `migrate --fresh` → `users create --admin` → логин через `/auth/login` реально сработал → `backup create` (архив содержит рабочий `db.sql` + `soar-data.tar.gz`) → `down`; поймал и починил реальный баг — `shutil.copytree()` тащил `soarctl_lib/__pycache__` в бандл, добавлен regression-тест. (2) Upgrade-сценарий (`v1` → `v2` поверх того же инстанса): `install` обновляет только `SOAR_VERSION` в `.env`, секреты (`AUTH_SECRET_KEY`/`POSTGRES_PASSWORD`/`SOAR_WEBHOOK_TOKEN`) не трогает; `up` пересоздаёт только `orchestrator`/`ui` (сменился тег), `postgres`/`redis` не перезапускаются; admin, созданный на v1, успешно логинится после апгрейда на v2 — Postgres-данные переживают апгрейд. Этой проверкой найден и исправлен баг в `deploy/prod/README.md`: шаг `migrate` был перед `up`, но `migrate` делает `docker compose exec orchestrator ...` в **текущем** (ещё старом) контейнере — правильный порядок `install → up → migrate`. Спек/план: `docs/compose/specs/2026-07-22-deploy-cli-design.md`, `docs/compose/plans/2026-07-22-deploy-cli.md`, отчёт: `docs/compose/reports/deploy-cli.md`.
- **v0.11** (2026-07-22) — Agent Dev-Loop Этап 1 (`UPGRADE.md`, закрывает P1/P2/P8/P9): замыкает цикл «агент написал код → узнал результат → откатился при ошибке» без чтения серверных логов/файловой системы. **P1** — `orchestrator/api/validation.py::validate_workflow_code/validate_action_code/validate_connector_code` (`ast.parse`, без импорта) перед записью файла в `PUT /workflows/{name}/code`, `/actions/{name}`, `/connectors/{name}/code` — 422 с текстом ошибки вместо тихого `{"status": "saved"}`, если код не парсится или не содержит ожидаемую точку входа. **P2** — `WorkflowResult.traceback` (`soar/workflows/base.py`), `soar/runner.py::main()` кладёт полный `traceback.format_exc()` в то же поле `"error"` результата (не новый ключ), и оборачивает весь вызов `workflows.execute()` в try/except — ошибка до входа в `run()` (workflow не найден, упал конструктор) тоже даёт структурированную JSON-строку, не голый crash subprocess. **P8** — `orchestrator/core/history.py` (общая обёртка над уже существовавшими `GitManager.history/get_content/diff/restore`, устраняет тройное дублирование) + `GET/GET/GET/POST .../history[/{commit}]`, `.../diff`, `.../restore` на workflows (code), actions, connectors (code и config отдельно) — read на `_RO`, restore на `_ADMIN` с `AuditLog`-записью; `GitManager.restore()` теперь принимает `author_name`/`author_email` как `commit()`. **P9** — `orchestrator/core/workflow_state.py` (новый единственный читатель/писатель `orchestrator_state.yaml`, заменяет инлайновое чтение в `main.py` и `_save_state`/`_remove_from_state` в `workflows.py`), формат состояния сменился со строки (`"enabled"`/`"disabled"`) на объект (`{enabled, token}`, обратно совместим со старым форматом и bool); `load_workflow_metas` предпочитает сохранённый webhook-токен токену из свежего импорта класса — повторное сохранение кода того же webhook-workflow больше не меняет его токен. 53 новых/расширенных теста, полный набор проходит без регрессий (за вычетом преэкзистентных сбоев — отсутствующие опциональные зависимости коннекторов, живой Redis, один пред-существующий провал в `test_openapi.py`, не связанные с этой веткой). Спек/план/отчёт: `docs/compose/specs/2026-07-22-agent-devloop-stage1-design.md`, `docs/compose/plans/2026-07-22-agent-devloop-stage1.md`, `docs/compose/reports/agent-devloop-stage1.md`.
- **v0.12** (2026-07-27) — Pre-release ревью перед деплоем на живую инфраструктуру (`docs/concepts/UPGRADE-v2.md`, закрывает P12/P13/P14/P16). **P12** — `soar/tools/http_client.py::HttpClient` (singleton, `soar/runner.py` инициализирует из `SOAR_CONFIG`): логирование каждого запроса безусловно через loguru (method/domain/status/duration_ms/cache_hit), кэш GET-ответов опционален (`http_client.cache_backend: memory | redis | none`, per-call `cached` флаг), `POST` никогда не кэшируется, `_validate_external_url` — отдельная от `orchestrator/api/connectors.py` реализация SSRF-защиты (raises `ValueError`, не `HTTPException` — `soar/` не зависит от `orchestrator/`). Заменяет неисполненную v0.6-спеку (`CachedHttpClient`). **P13** — `GET /connectors/{name}/schema` (типизированные поля конструктора + `hidden: bool`, AST-интроспекция расширяет `orchestrator/core/introspect.py`), явный class-level `HIDDEN_FIELDS` на всех 24 коннекторах; `GET /config`, `/config/history[/{commit}]`, `/config/diff` маскируют значения hidden-полей `"********"` для всех ролей включая `admin` (write-only секреты); `PUT /config` — merge-on-write (плейсхолдер не затирает старое значение) + реальное изменение hidden-поля требует роль `admin` буквально (`403` для `agent`), не-hidden поля правят обе роли как раньше; `ui/src/views/Connectors.vue` — форма по схеме вместо raw-textarea, hidden-поля как всегда-пустые password-инпуты, fallback на textarea если схема пуста. **P14** — `SQLQueue` (`orchestrator/core/queue/sql_queue.py`) — poll-based очередь поверх уже существующей `workflow_jobs` (`push()` no-op, `pop()` — атомарный claim, `FOR UPDATE SKIP LOCKED` на Postgres / сериализация на SQLite), устраняет at-most-once потерю джобов `RedisQueue`; валиден только с `jobs.persistence: sql` (fail-fast `ValueError` иначе); partial-индекс `(status, triggered_at) WHERE status='PENDING'` держит claim дешёвым; новый `jobs.retention_days` (дефолт `0`) + периодическая очистка через `OrchestratorScheduler`; `deploy/prod/config.yaml.template`/`deploy/stage/config.yaml` переключены на `queue.backend: sql`. **P16** — `GitManager.commit()` определяет "нечего коммитить" через exit-код `git diff --cached --quiet` вместо string-match по stderr git'а — чинит потерю audit-записи, когда рабочая директория содержит untracked-файлы (`__pycache__` и т.п.), закрывает known-limitation #7. **P15/P17** приняты как остаточный риск/операционный чеклист без изменений кода (recovery последнего admin — через `orchestrator/auth/cli.py`; CORS origins — явный плейсхолдер в `config.yaml.template` + шаг чеклиста в `deploy/prod/README.md`). Полный набор тестов проходит без регрессий на каждом этапе мёржа (за вычетом тех же преэкзистентных сбоев — живой Redis, один пред-существующий провал в `test_openapi.py`). Спеки/планы/отчёты: `docs/compose/specs/2026-07-27-{http-client,connector-secrets-schema,sql-job-queue,git-manager-nothing-to-commit}-design.md`, `docs/compose/plans/2026-07-27-*.md`, `docs/compose/reports/{http-client,connector-secrets-schema,sql-job-queue,git-manager-nothing-to-commit}.md`.
- **v0.13** (2026-07-27) — `soarctl` on-site install + update, для деплоя на машине с доступом в интернет (без air-gap bundle). `soarctl install --repo <url-or-path> [--ref REF]` — второй источник для `install` наряду с bundle-tar: локальный путь используется как чекаут напрямую, URL клонируется в `<dest>/src`; версия образа — `git describe --tags --always --dirty`; сборка через вынесенный из `bundle.package()` общий `bundle.build_images()` (без `docker save`/`docker load` — образы остаются локально). `soarctl init --interactive`/`--cors-origin URL` — `config.yaml.template` получил настоящий `${CORS_ORIGINS_JSON}` вместо захардкоженного плейсхолдера; `env.init_instance()` принимает `overrides`, накладываемые поверх дефолта — частичное закрытие P17 кодом (было — только чеклист в README). Новая команда `soarctl update [--ref REF] [--migrate fresh|upgrade]` — только для git-инстансов (маркер `source.json` от `install --repo`): `git fetch`+`checkout`/`pull --ff-only`, пересборка образов, `env.update_version()`, `compose up` — без `docker compose down`; postgres/redis не пересоздаются, поскольку их теги образов не меняются. Миграции по-прежнему без авто-детекта — `--migrate` явный, без флага `update` ничего не применяет. `soarctl doctor` — новая проверка `git checkout` (только когда `source.json` присутствует). 36 новых тестов (`tests/deploy/test_soarctl_{bundle,env,prompts,git_source,doctor,cli}.py`), полный набор `tests/deploy/` (106) и `ruff check` без регрессий; air-gap bundle-путь не изменён. Заодно найден и починен реальный деплой-баг из v0.12 (P12): `soar/tools/http_client.py` безусловно импортирует `httpx` через `soar/tools/__init__.py` (цепочка `orchestrator/api/connectors.py` → `soar.tools.openapi` → `soar.tools`), но `httpx` был только в `soar/requirements.txt`, а оба `Dockerfile.orchestrator` (`deploy/stage/`, `deploy/prod/`) ставят лишь `orchestrator/requirements.txt` + вручную выбранный список (`elasticsearch vt-py requests`) — оркестратор падал на импорте при старте с чистого образа. Добавлен `httpx` в оба списка. Обнаружено и подтверждено вживую: пересборка `deploy/stage` (стенд не пересобирался 3 недели, поэтому баг не проявлялся раньше) → `docker compose up --build` → падение на `ModuleNotFoundError: No module named 'httpx'` → фикс → все 4 контейнера healthy, `alembic stamp head` на свежем Postgres (`table_prefix: stage_`), `GET /health` ok. Полный набор тестов (`tests/`) прогнан с живым Docker: 686 passed, 1 skipped, 1 pre-existing/несвязанный fail (`test_openapi.py::test_generate_config`) — три `test_redis_integration.py` теста, ранее пропускавшиеся без Redis, теперь тоже проходят. Спек/план/отчёт: `docs/compose/specs/2026-07-27-soarctl-onsite-update-design.md`, `docs/compose/plans/2026-07-27-soarctl-onsite-update.md`, `docs/compose/reports/soarctl-onsite-update.md`.
- **v0.24** (2026-08-06) — Egress policy: allowlist in config instead of unconditional deny-private. Found on a live run: connecting to a SIEM on `192.168.1.51` via the official `elasticsearch` client (bypassing `http_client`, straight through `urllib3`) failed with `PermissionError: egress to private address 192.168.1.51 blocked by audit hook` — there was no way to allow a private address anywhere (config, env, or API). This wasn't a missing setting, it was a threat-model mistake (`ENTITY-MODEL.md` decision 2): deny-all-private is correct when the private range is someone else's infrastructure (SaaS), but for an on-prem SOC the private range *is* the entire working surface — SIEM, AD, FreeIPA, TrueConf are all on it, so the platform couldn't reach any internal system at all. New `soar/egress_policy.py` (`EgressPolicy`, `parse()`, `is_allowed()`) reads a `egress` config section (`mode: allowlist|observe`, `allow: [CIDR, ...]`) — `allowlist` (default) keeps today's deny-private behavior with explicit CIDR exceptions (empty `allow` = unchanged behavior, upgrade opens nothing silently); `observe` is a documented escape hatch that logs without blocking, needed because on-site installs build their own images (`git_source.update()`) and would otherwise hand-patch `audit_hook.py` invisibly instead. One parsed policy, two enforcement points: `soar/audit_hook.py::install(policy)` now takes the policy as a closure argument instead of reading a hardcoded rule (content in the same process could never rewrite it, since the name isn't a module global); `soar/tools/_net.py::_validate_external_url` consults the same semantics via its own `set_policy()`, called from `soar/runner.py::_build_http_client` before any content loads — so the hook (layer-2 enforcement) and `http_client`'s pre-flight `ValueError` never disagree on what's allowed. `soar/runner.py`'s config load now happens *before* `install_audit_hook()` (was after) so the real policy is available at hook-install time; a malformed `egress` section (bad `mode`, unparseable CIDR) raises loudly at import time, not a silent fallback to deny. `orchestrator/core/subprocess_runner.py::build_scoped_config` copies `egress` into the job's scoped config the same way it already does `http_client` — the one place a missed step would have produced a silently-inert setting. `GET /runtime` gained an `egress: {mode, allow}` block (new `EgressConfig` in `orchestrator/config.py`), readable by role `agent` — the system prompt now tells the agent egress is a platform-wide restriction (any library, not just `http_client`) and to check `GET /runtime` before writing a connector to an internal system. `docs/concepts/ENTITY-MODEL.md` decision 2 amended in place rather than rewritten — the isolation model (hook can't be removed, still denies by default) is unchanged, only what can be configured. Verified end-to-end against a real `python -m soar.runner` subprocess (not mocks): a workflow connecting to an allowlisted loopback address succeeds, the same workflow connecting to a private address outside the allowlist gets `PermissionError` from the audit hook, both logged. 40 new/updated tests (`tests/soar/test_egress_policy.py`, `tests/soar/test_audit_hook.py`, `tests/soar/tools/test_egress_net.py`, additions to `tests/soar/test_runner.py`/`tests/orchestrator/test_subprocess_runner_env.py`/`tests/orchestrator/api/test_runtime.py`), full suite 851 passed / 3 pre-existing Redis-integration failures (no live Redis, unchanged from v0.23) / 9 skipped, `ruff check` clean on all touched files. Spec/plan/report: `docs/compose/specs/2026-08-06-egress-policy-design.md`, `docs/compose/plans/2026-08-06-egress-policy.md`, `docs/compose/reports/egress-policy.md`.
- **v0.23** (2026-08-06) — Connector ownership split: code to the agent, credential values to the operator. Reverses B3 (`2026-07-28-connector-code-agent-lockdown-design.md`, `PUT /connectors/{name}/code` pinned to a literal `admin`) after field feedback — an agent wrote a real connector and could not save it, having to hand multi-line Python to a human with an admin key through Swagger UI. The barrier was also **not holding**: `POST /connectors/{name}` (`_ADMIN`, agent-allowed) commits `CONNECTOR_TEMPLATE` with an empty `HIDDEN_FIELDS`, and `POST /{name}/code/restore` stayed on `_ADMIN` — so the agent could roll back to its own template commit and strip redaction without ever calling `PUT`. B3's reasoning ("every version in history was written by an admin `PUT`") never accounted for the template commit. Three changes, none of which works alone. **(1) Roles** — `PUT /{name}/code` back to `_ADMIN`; new `_CONFIG_RO` (`_RO` minus `agent`) on `GET /{name}/config`, `/config/history/{commit}`, `/config/diff`; literal `admin` on `PUT /{name}/config` and `POST /{name}/config/restore`. Config write was taken away together with config read deliberately: `PUT` writes the whole file, so a blind write would clobber values the writer cannot see. The agent keeps `GET /{name}/schema` (field names, types, `hidden` flag — no values) and `GET /{name}/config/history` (that the config changed, not what is in it). **(2) Redaction fails closed** — `_hidden_fields_for` now returns `set[str] | None`, where `None` ("policy unreadable": missing `.py`, `SyntaxError`, no classes) is distinct from an empty set ("declares no secrets", what `CONNECTOR_TEMPLATE` states explicitly); on `None`, `_redact_yaml`/`_redact_diff` mask **every** value under `instances.*` instead of none. Previously any path leaving an unparseable `.py` next to a live `.yml` served credentials in the clear — including through `POST /transfer/export`, which shares these functions. **(3) `HIDDEN_FIELDS` is monotonic for non-admins** — the alternative B3 costed and deferred ([S3] there) is now implemented: `_assert_hidden_fields_not_narrowed` requires `new >= old` for every role but `admin`, and is applied at **all three** code-write paths — `PUT /code`, `POST /code/restore` (the target commit's content is parsed before the rollback) and `POST /generate` (overwriting an existing connector). Covering every write path to the policy carrier, not just one, is the actual lesson of B3. `parse_classes_source` split out of `parse_classes` (`orchestrator/core/introspect.py`) and `OpenAPIGenerator.render_class` added so a write can be checked before anything reaches disk. UI mirrors the backend tuples (`ui/src/permissions.js`, new `connector.config.read` capability; the Connectors "Setup" button is hidden for roles that cannot read config). **Explicitly not a security boundary, and documented as such** (Known Limitations #9, follow-up spec `2026-08-06-connector-secret-runtime-boundary-design.md`): role `agent` already has arbitrary code execution (`PUT /workflows/{name}/code` + `POST /jobs`), the job subprocess gets credentials as plaintext YAML, `ConnectorProxy.__getattr__` hands back raw values for anything non-callable or underscore-prefixed (`conn.api_key`, `conn._instance` — contradicting that file's own docstring), and job logs are served unredacted — so `print(conn.api_key)` plus `GET /logs/{id}` yields a credential today, unchanged by this release either way. 12 new/updated tests (`tests/orchestrator/api/test_connector_hidden_fields_integrity.py` — narrowing via each path, the create→restore bypass as an explicit regression, fail-closed on broken/missing `.py`; plus the RBAC-shift cases in `test_agent_role_rbac.py`), 831 passed / 3 pre-existing Redis-integration failures (no live Redis) / 9 skipped, UI 85 passed. Spec/plan/report: `docs/compose/specs/2026-08-06-connector-code-agent-unlock-design.md`, `docs/compose/plans/2026-08-06-connector-code-agent-unlock.md`, `docs/compose/reports/connector-code-agent-unlock.md`.
- **v0.22** (2026-08-05) — Docs-only: `orchestrator/prompts/system_prompt.md` (agent's built-in self-description, `GET /prompts/system`) rewritten — last revised v0.15 (2026-07-29), four releases (v0.17-v0.21) had drifted the facts it states. Fixed: entity storage paths (§2) now reference the configured `connectors_dir`/`actions_dir`/`workflows_dir` instead of stale hardcoded `soar/...` paths from before ENTITY-MODEL Phase 3's content-pack split. New §3 "Using a connector from your code" — the file never documented either connector-import form (`from soar.connectors.<type> import <instance>` vs `from soar.connectors import connectors`), or why the former is preferred: import-time failure instead of a mid-job `AttributeError`, and — the load-bearing reason — it's what Phase 4's credential scoping reads to decide which connector instances' config a job's subprocess receives. `GET /runtime` (shipped in Phase 3, never mentioned) added to §6 self-description — the direct answer to "what can I import." §6's `/tools` examples updated for the 2026-08-03 redesign (`LoggingHttpClient`/`CachingHttpClient`/`new_client`/`WatermarkStore`/`SeenStore`, current `TOOL_REGISTRY`, replacing the now-nonexistent generic `HttpClient` example). One risk item added (§8): `PUT .../code` validates syntax/entry-point only, not import resolution — an unresolved connector instance or out-of-contract package surfaces only as a job traceback, not at write time. RBAC boundary (§4) and dev-loop mechanics (§5) re-verified against current route definitions, carried over unchanged in substance; `agent`-excluded endpoint list gained `POST /connectors/pack/install` (didn't exist at v0.15). `tests/orchestrator/api/test_prompts_api.py` unaffected (content-agnostic, 5 passed). Spec/plan/report: `docs/compose/specs/2026-08-05-system-prompt-rewrite-design.md`, `docs/compose/plans/2026-08-05-system-prompt-rewrite.md`, `docs/compose/reports/system-prompt-rewrite.md`.
- **v0.21** (2026-08-03) — Tools: явный реестр, изоляция внутренностей, HTTP-клиент без async/JSON-only. `soar/tools/__init__.py::TOOL_REGISTRY` (literal dict: `kind` = `class`/`instance`/`factory`) replaces the `__all__`-only declaration — it names not just what's public but how to introspect it, closing the gap that made `GET /tools` return a synthetic `{"summary": ""}` stub for every non-class singleton/factory (`http_client`, `watermark_store`, `seen_store`, ...). `orchestrator/core/introspect.py::parse_tool_registry` reads it via AST (no import); `orchestrator/api/tools.py::_resolve` dispatches by `kind` — an entry that still fails to resolve (broken tool file) surfaces as `{"error": "unresolved"}` + `logger.error`, never a silent stub, since the registry covers 100% of public names by construction. `HttpClient`/`SyncHttpClient` (the async/sync pair, `soar/tools/http_client.py`) are gone — dead async code (`grep` across `soar-content-pack` found zero `await http_client.` calls) that doubled the module's surface for no runtime benefit (`soar/workflows/base.py::BaseWorkflow.run` is entirely synchronous). `LoggingHttpClient`/`CachingHttpClient` subclass `httpx.Client` directly and override `send()` — one interception point instead of hand-duplicated `get_json`/`post_json`/`put_json`, covering every httpx method and both JSON (`.json()`) and binary (`.content`, e.g. pcap) responses; log line is written before `raise_for_status()` so error responses still log. `new_client(verify=...)` gives connectors with non-default TLS trust or persistent state (cookie jar) their own instance sharing the same cache/ttl config as the `http_client` singleton, instead of a fresh unwired one. `soar/runner.py` now builds one client, not two. Internal mechanics moved to `soar/tools/_cache.py` (`CacheBackend`/`InMemoryCache`/`RedisCache`) and `soar/tools/_net.py` (SSRF guard) — the existing `_*.py`-is-internal convention (already free for `GET /tools`) now applies here too. All 10 HTTP connectors in `soar-content-pack` migrated: `abusech`/`censys`/`crtsh`/`fofa`/`urlhaus` onto the shared `http_client` singleton; `rstcloud`/`kaspersky_opentip`/`wazuh`/`security_onion`/`freeipa` onto `new_client(verify=self.verify_ssl)` — `freeipa` in particular drops its hand-rolled login `httpx.Client` and manual `Cookie` header forwarding entirely, since `self._client`'s cookie jar now persists across the login call and every subsequent JSON-RPC call on its own. Found and fixed in passing (pre-existing, unrelated to this spec): all 24 `connectors/*/__init__.py` in `soar-content-pack` still imported from the pre-Phase-3 `soar.connectors.<name>.<name>` path, breaking every connector's own test collection — fixed for the 10 connectors in scope here, the other 14 are a separate, not-yet-filed bug. 812 passed (soar), 108 passed / 5 pre-existing collection errors from the `__init__.py` bug above, unrelated connectors (soar-content-pack), `ruff check` clean on all touched files. Spec/plan/report: `docs/compose/specs/2026-08-03-tools-redesign-design.md`, `docs/compose/plans/2026-08-03-tools-redesign.md`, `docs/compose/reports/tools-redesign.md`.
- **v0.20** (2026-07-30) — Entity model Phase 4: privilege narrowing (`docs/concepts/ENTITY-MODEL.md`, isolation layer 3). Credential scoping (always on): `orchestrator/core/introspect.py::parse_connector_usage` statically scans (AST, no import) `from soar.connectors.<type> import <instance>` at workflow module top-level; `orchestrator/core/subprocess_runner.py::build_scoped_config` builds a temp `connectors_dir` slice from that list (symlinked `.py`, filtered `.yml`) and hands the job subprocess a temp `SOAR_CONFIG`, never the orchestrator's full `config.yaml` (JWT secret, `database.url` never reach the runner). Workflows with no statically-found imports get an empty `connectors_dir`, not a fallback to the full set — deliberate, since no workflow in this repo uses the pre-Phase-2 `connectors.<name>` registry form. The scoped temp dir (`WorkflowJob.scoped_config_dir`, new field — also fixes `WorkflowMeta.file_path`, present since Phase 1 but never populated) is removed in `Worker._execute`'s `finally` on every exit path, and threaded through `SQLQueue`/`RedisQueue` serialization too (new `workflow_jobs.workflow_file` column, migration `7a1c9e3f5b02`), not just `InMemoryQueue`. Separate runner UID + `RLIMIT_AS`/`RLIMIT_CPU`/`RLIMIT_NPROC` (`jobs.runner_uid`, `None` default — opt-in, POSIX/Docker-only) uses a different mechanism than the original spec pseudocode: a direct `os.setuid`/`os.setgid` in `preexec_fn` does not work from a non-root parent (the orchestrator runs as `soar`, not root) without granting `CAP_SETUID` to the interpreter itself, which would be a bigger privilege grant than intended (verified in Docker — `docker run --cap-add=SETUID/SETGID` only populates the bounding set, not a non-root process's effective set). Instead, argv is wrapped in `setpriv --reuid=... --regid=...`, with the file capability (`setcap cap_setuid,cap_setgid+ep`) scoped to that small `util-linux` binary alone; rlimits still go through `preexec_fn` (no capability needed to lower your own limits). New `soar-runner` user (fixed uid/gid `5001`) in `deploy/{prod,stage}/Dockerfile.orchestrator`, `config.yaml` mode 640 (`soar:soar`), `/app/data/state` group-writable by `soar-runner` (watermark/seen-store writes). All boundaries — config.yaml unreadable, git repo unwritable, state_dir writable, RLIMIT_AS enforcement (real `MemoryError` on overshoot) — verified against real Linux containers via Docker, not mocks only. Explicitly deferred: a full `deploy/stage` `docker compose up` run with `jobs.runner_uid` flipped on and a real job through the API — see the report for what a human should check before enabling this on live infra. 17 new tests (`parse_connector_usage`, `build_scoped_config`, scoped-dir cleanup across success/timeout/exception, `_drop_privileges`/`_runner_argv` gating — the last skipped on Windows dev, run for real against a Linux container in this session), full suite 809 passed / 3 pre-existing Redis-integration failures unchanged / 9 skipped (8 new POSIX-only), `ruff check` clean on all touched files. Spec/plan/report: `docs/compose/specs/2026-07-30-privilege-narrowing-design.md`, `docs/compose/plans/2026-07-30-privilege-narrowing.md`, `docs/compose/reports/privilege-narrowing.md`.
- **v0.19** (2026-07-30) — Entity model Phase 3: content as contentpack (`docs/concepts/ENTITY-MODEL.md`). All 24 built-in connectors moved out of `soar/connectors/<name>/` into a separate, local-only (no remote) sibling git repository `soar-content-pack` — `soar/connectors/` now holds only `__init__.py`/`base.py`/`_proxy.py`, the structural duplicate (E1) is physically gone, not just addressed by API precedence. One install pipeline (`orchestrator/core/pack_install.py`: manifest reading, `runtime_version`/dependency checks against `soar/runtime_contract.py::CONTRACT`, sha256-based `.soar-content.yaml` origin marker, plan/apply that leaves user-modified connectors untouched) backs three entry points: `soarctl content install|list|remove` (`deploy/soarctl_lib/content.py`, alpine tar-pipe against the `soar-data` volume, same pattern as `backup.py`), `POST /connectors/pack/install` (`orchestrator/api/packs.py`, admin-only, conflict-preflight+`force`, audit trail), and idempotent seeding on every orchestrator startup (`orchestrator/main.py::seed_connector_pack` replaces the old one-time `shutil.copytree` — closes **E4**, new connectors in a later release now reach existing installs via `soarctl update`). The pack's manifest is generated by an AST script (`soar-content-pack/tools/gen_manifest.py`), never hand-written. Both Dockerfiles gained `COPY --from=basepack` from the sibling repo as an extra build context — verified with a real `docker compose build` and a live container: all 24 connectors install and import correctly from the content venv. 792 passed (was 894 — expected drop: 145 connector tests moved with their code into `soar-content-pack/tests/`), `ruff check` clean of new findings. Spec/plan/report: `docs/compose/specs/2026-07-30-content-as-contentpack-design.md`, `docs/compose/plans/2026-07-30-content-as-contentpack.md`, `docs/compose/reports/content-as-contentpack.md`.
- **v0.18** (2026-07-30) — Entity model Phase 2: entity model in code (`docs/concepts/ENTITY-MODEL.md`, E6+E3 done together per decision 4). `ConnectorRegistry` namespaced by type (`dict[type][instance]` instead of a flat dict, closes **E8**), deterministic class discovery (`obj.__module__ == fqn`, mirroring `WorkflowRegistry`). `ConnectorProxy` (`soar/connectors/_proxy.py`) is the only way to get a connector — via the existing `connectors.<name>` registry attribute or the new lazy `from soar.connectors.<type> import <instance>` shim (closes **E6**) — both facades hand out the wrapped object, so direct import can never become a hole around logging. Every public method call writes a `SOAR_AUDIT_EVENT` line to the job log (`HIDDEN_FIELDS` redaction, duration, outcome); `context["dry_run"]` blocks methods declared in a new `MUTATING_METHODS` class attribute centrally on the proxy, not by voluntary workflow convention; `Worker._execute` parses these lines into `AuditLog` rows (closes part of **E3** — what a job did to an external system is now visible). `soar/actions/__init__.py` registers every public top-level callable in a file, not only the one matching the filename; `GET /actions` stays AST-only (never imports content from the orchestrator process, a Phase 1 requirement) and reflects exactly what the registry can run (closes **E7**). `soar/tools/__init__.py::__all__` is the single source `GET /tools` reads; `OpenAPIGenerator` moved to `orchestrator/core/` (not a runtime tool), `watermark_store`/`seen_store` factories follow the `http_client` singleton pattern (closes **E5**). The remaining 7 `requests`/`httpx` connectors (`censys`/`crtsh`/`fofa`/`freeipa`/`security_onion`/`urlhaus`/`wazuh`) migrated onto `http_client_sync`. 894 passed (was 808), `ruff check` clean of new findings. Spec/plan/report: `docs/compose/specs/2026-07-30-entity-model-in-code-design.md`, `docs/compose/plans/2026-07-30-entity-model-in-code.md`, `docs/compose/reports/entity-model-in-code.md`.
- **v0.17** (2026-07-30) — Entity model Phase 1: runtime boundary (`docs/concepts/ENTITY-MODEL.md`). Two venvs baked into the image — `platform-venv` (`orchestrator/requirements.txt`, first on `PATH`) and `content-venv` (`soar/requirements.txt`, actually installed for the first time — closes **E2**, 11 of 24 built-in connectors were missing packages); `SubprocessRunner` runs workflows through `content-venv` (`SOAR_CONTENT_PYTHON`, falls back to `sys.executable` outside Docker/tests). `soar/runtime_contract.py` is the versioned dependency contract (import name + protocol/vendor kind), the single source for both installation and `GET /runtime` (new endpoint — environment over API, packages by import name, guaranteed/present_not_guaranteed split, closes **E9**). `orchestrator/main.py::load_workflow_metas` no longer imports user workflow code — AST parsing instead (`orchestrator/core/introspect.py::parse_workflow_meta`), closes **E10.3**, a hard requirement after the runtime split, not just hygiene. An audit hook (`soar/audit_hook.py`, `sys.addaudithook`) installs in `soar/runner.py` before any `init()` — observes and can block egress to private addresses regardless of which library triggered it; the existing SSRF guard in `http_client.py` is untouched (stays a pre-flight check, the hook is the platform-level guarantee on top). A real Docker build+run caught and fixed two defects invisible to unit tests: `httpx` was missing from `orchestrator/requirements.txt` (a transitive need via `soar.tools.openapi`), and `GET /runtime` was resolving the `content-venv/bin/python` symlink all the way to the base interpreter (`.resolve()` → `.absolute()`). 811 passed (was 780), `ruff check` clean of new findings. Spec/plan/report: `docs/compose/specs/2026-07-30-runtime-boundary-design.md`, `docs/compose/plans/2026-07-30-runtime-boundary.md`, `docs/compose/reports/runtime-boundary.md`.
- **v0.16** (2026-07-29) — `soarctl` on-site rework: the checkout is the instance, no copied-out directory. Reworks `docs/compose/specs/2026-07-27-soarctl-onsite-update-design.md`, which left `install --repo`/`update` copying `docker-compose.yml`/`config.yaml.template` into a separately-named `--dir` directory with no structural link to the checkout (and never copied `soarctl` itself there — the documented `cd soar-prod && python soarctl doctor` step would have failed with "file not found", never exercised end-to-end). Now `git_source.install(checkout, ref)` writes `VERSION`/`source.json` directly into `<checkout>/deploy/prod/` — the compose file and config template are already there, tracked in git, nothing to copy. `--repo <url>` (soarctl cloning for you) is removed entirely — clone yourself, `soarctl` only operates on an existing checkout. `paths.instance_dir()` now walks up from cwd the way `git` finds `.git` — `docker-compose.yml` directly present (bundle) or `pyproject.toml` + `deploy/prod/docker-compose.yml` (on-site) — so every subcommand works from any subdirectory of the checkout, not just its root. New root-level `./soarctl` bash wrapper (executable bit set via `git update-index --chmod=+x`, `.gitattributes` pins LF) drops the `python deploy/soarctl` prefix. End result: `git clone <url> soar && cd soar && ./soarctl install && ./soarctl init && ./soarctl up`. Air-gapped bundle path (`package`/`install <bundle.tar.gz>`) unchanged. 115 tests in `tests/deploy/` (up from 106), `ruff check` clean. Spec/plan/report: `docs/compose/specs/2026-07-29-soarctl-inplace-onsite-design.md`, `docs/compose/plans/2026-07-29-soarctl-inplace-onsite.md`, `docs/compose/reports/soarctl-inplace-onsite.md`.
- **v0.15** (2026-07-29) — Docs-only fix: `orchestrator/prompts/system_prompt.md` (встроенный системный промпт агента, `GET /prompts/system`) не обновлялся с Agent Dev-Loop Этапа 2 (2026-07-22) и содержал устаревшее утверждение, что `GET /connectors/{name}/config` отдаёт секреты в открытом виде — фактически с v0.12 они маскируются `"********"` для всех ролей включая `admin`. Переписан раздел про секреты на актуальное write-only/merge-on-write поведение; добавлена секция "Your access (role `agent`)" — граница роли (код+jobs, не администрирование: `/auth/*`, `/audit-log`, `/transfer/*`, `PUT /prompts/user` недоступны) и явное исключение B3 (`PUT /connectors/{name}/code` — литеральный `admin`, единственный write-эндпоинт, где `agent` исключён); `Tool` добавлен в список сущностей (§2) как явно read-only — раньше упоминался только в self-description, не был назван сущностью. Спек/план/отчёт: `docs/compose/specs/2026-07-29-system-prompt-refresh-design.md`, `docs/compose/plans/2026-07-29-system-prompt-refresh.md`, `docs/compose/reports/system-prompt-refresh.md`.
- **v0.14** (2026-07-28) — Закрыт `docs/concepts/BAGFIX_PLAN.md` целиком: все 4 блокера пилота (B1-B4), все 8 существенных пункта (S1-S8) и все 8 расхождений документации (D1-D8) из `docs/compose/reports/prod-readiness-review-2026-07-27.md`. **B1** — `rotate_refresh_token()` теперь проверяет `User.is_active`, `update_user()`/`set_user_active()` отзывают все живые refresh-токены при деактивации или смене роли — деактивация реально закрывает доступ, не только логин. **B2** — `_DIFF_KV_RE` в `orchestrator/api/connectors.py` теперь матчит и контекстные строки unified diff (ведущий пробел), не только `+`/`-` — `GET /config/diff` больше не палит hidden-поля роли `viewer` через соседнюю правку. **B3** — `PUT /connectors/{name}/code` — литеральный `admin`, не `_ADMIN`: `agent` больше не может обнулить `HIDDEN_FIELDS` коннектора, переписав его код. **B4** — `deploy/prod`/`deploy/stage` `docker-compose.yml` получили выделенную сеть со статическим IP для `ui` (nginx), `server.trusted_proxies` в обоих конфигах указывает на этот IP — login-лимитер и `AuditLog.client_ip` больше не схлопываются в один IP на весь трафик через прокси. **S1** — `SyncHttpClient` (`soar/tools/http_client.py`) — синхронный близнец `HttpClient` над `httpx.Client`, тот же контракт логирования/кэша/SSRF-guard; `abusech`/`rstcloud`/`kaspersky_opentip` мигрированы на него как образец. **S2** — сборка `tools.http_client`/`tools.http_client_sync` в `soar/runner.py` перенесена перед `workflows.init()`/`connectors.init()`/`actions.init()` — верхнеуровневый `from soar.tools import http_client` в коннекторе/action больше не захватывает неконфигурированный дефолт. **S3** — `POST /transfer/export` редактирует hidden-поля (`_redact_yaml`, переиспользован из `connectors.py`), `/export` и `/import` пишут `audit_log`, `/import` валидирует код через `validate_*_code` и коммитит импортированные файлы в git перед тем, как раньше писал их напрямую. **S4** — `POST /jobs` и `POST /webhooks/{name}` пишут `job.create` в audit-лог; вебхук — синтетический актор `CurrentUser(id=0, role="service", type="webhook", username=f"webhook:{name}")`, третий `actor_type` кроме `user`/`service`. **S5** — `SQLJobStore.purge_old()` удаляет файлы логов вместе со строками БД (`log_path` собирается до `DELETE`, файлы — после коммита; ошибка удаления файла — `logger.warning`, не исключение). **S6** — партиальный индекс `workflow_jobs(status, triggered_at) WHERE status='PENDING'` теперь объявлен и в `JobRecord.__table_args__` (создаётся `create_all()` на любой свежей инсталляции, не только через `alembic upgrade head`); миграции `42fbd47b0d46`/`3067dea7c75b` используют `prefixed()` вместо литеральных имён таблиц/индексов. **S7** — `OpenAPIGenerator._generate_config()` использует голое snake_case имя коннектора как ключ инстанса (не `f"{ClassName}1"`) — тест-сьют полностью зелёный; 5 тестовых модулей коннекторов (`pymisp`/`pymysql`/`shodan`/`winrm`/`smbprotocol` — не `impacket`, как думал обзор) получили `pytest.importorskip`. **S8** — `CONNECTOR_TEMPLATE` объявляет `HIDDEN_FIELDS: ClassVar[set[str]] = set()`, `OpenAPIGenerator` автоматически заполняет `HIDDEN_FIELDS` именами auth-полей из `securitySchemes` (`apiKey`/`bearer`/`basic`, кроме `username`) — новые коннекторы больше не рождаются с редакцией секретов по умолчанию выключенной. Каждый пункт реализован отдельным циклом спека→план→отчёт в изолированной git-ветке (subagent + `git worktree`), смерджен в `main` после прогона тестов; полный набор `tests/` — `756 passed, 1 skipped` (skip преэкзистентный, не связан с этой веткой), `ruff check` — без новых находок. Спеки/планы/отчёты: `docs/compose/specs/2026-07-28-*-design.md`, `docs/compose/plans/2026-07-28-*.md`, `docs/compose/reports/{auth-deactivation-revocation,connector-diff-redaction-fix,connector-code-agent-lockdown,trusted-proxies,http-client-sync-facade,http-client-init-order,transfer-export-import-hardening,job-webhook-audit-logging,job-log-purge,workflow-jobs-index-table-prefix,test-suite-green,new-connector-hidden-fields-default}.md`.
